apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: healthcare
  labels:
    app: frontend
    app.kubernetes.io/part-of: healthcare-system
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
