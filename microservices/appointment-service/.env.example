# Appointment microservice — copy to .env and adjust values
PORT=5001
NODE_ENV=development

# Must match backend JWT_SECRET so tokens from /auth/login validate here
JWT_SECRET=replace-with-a-long-random-secret

# Local (outside Docker)
MONGODB_URI=mongodb://localhost:27017/healthcare

# Docker Compose (inside containers)
# MONGODB_URI=mongodb://mongodb:27017/healthcare

# MongoDB Atlas (cloud production)
# MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/healthcare?retryWrites=true&w=majority

# Spec allowlist — frontend origin only (comma-separated)
CORS_ORIGINS=http://localhost:3000

# Cloud — Netlify site origin
# CORS_ORIGINS=https://your-site.netlify.app
